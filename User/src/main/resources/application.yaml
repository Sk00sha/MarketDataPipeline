server:
  port: 8080

spring:
  application:
    name: User
  datasource:
    url: jdbc:postgresql://localhost:5432/users
    username: skoosha
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true